/**
 * Адаптер над визуальным индикатором офлайн-режима.
 *
 * Переэкспортирует инициализацию общего индикатора, чтобы приложение
 * зависело от локального модуля, а не от внешнего пакета напрямую.
 */
export { initOfflineIndicator } from "@aris/offline";
